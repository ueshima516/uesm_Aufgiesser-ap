// TODO 項目を取得するAPIが引数
function PulldownGetItems() {
    // return Array.from({ length: 666+1 }, (_, i) => i);
    return ["🧀", "😻", "💩"];
}

export default PulldownGetItems;
